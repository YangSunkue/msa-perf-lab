syntax = "proto3";

package proto;

option go_package = "gocore/pkg/proto;proto";

service CoreService {
    rpc Ping (PingRequest) returns (PingResponse);
}

message PingRequest {
    string payload = 1;
}

message PingResponse {
    string reply = 1;
}