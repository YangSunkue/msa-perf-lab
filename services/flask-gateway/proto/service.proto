syntax = "proto3";

package msaperflab;

option go_package = "go-core/proto;proto";

service CoreService {
    rpc Ping (PingRequest) returns (PingResponse);
}

message PingRequest {
    string message = 1;
}

message PingResponse {
    string reply = 1;
}