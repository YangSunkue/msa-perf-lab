syntax = "proto3";

package proto;

option go_package = "gocore/proto;proto";

service CoreService {
    rpc Ping (PingRequest) returns (PingResponse);
}

message PingRequest {
    string message = 1;
}

message PingResponse {
    string reply = 1;
}